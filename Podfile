# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RequestProject' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RequestProject

  target 'RequestProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RequestProjectUITests' do
    # Pods for testing
  end

pod 'Alamofire'

end
